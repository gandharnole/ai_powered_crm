import pandas as pd
import numpy as np

np.random.seed(42)

months = pd.date_range(start="2023-01-01", end="2024-12-01", freq="MS")
car_models = ['Swift', 'Baleno', 'Brezza', 'Alto']
data = []

for month in months:
    for car in car_models:
        marketing_spend = np.random.randint(100, 500)  # in INR thousands
        economic_index = np.random.uniform(0.7, 1.2)    # normalized economy index
        base_sales = {
            'Swift': 450,
            'Baleno': 400,
            'Brezza': 300,
            'Alto': 500
        }[car]
        noise = np.random.normal(0, 30)
        units_sold = int((base_sales + noise) * (marketing_spend / 300) * economic_index)

        data.append({
            'month': month.strftime('%Y-%m'),
            'car_model': car,
            'marketing_spend': marketing_spend,
            'economic_index': round(economic_index, 2),
            'units_sold': units_sold
        })

df = pd.DataFrame(data)
df.to_csv('maruti_sales_data.csv', index=False)

print("Dummy dataset created!")
